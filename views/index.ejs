<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <!-- Linking bootstrap to the file -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        /* All custom styles needed beyond Bootstrap */
        body {
            font-family: sans-serif;
            margin: 20px;
        }
        .auth-links a, .user-info a {
            text-decoration: none;
        }
        .user-info span {
            font-weight: bold; 
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">Welcome to the Home Page</h1>
        <% if (!loggedIn) { %>
            <!-- If user is not logged in two links display to other pages-->
            <div class="d-flex justify-content-center mt-4">
                <a href="/signup" class="btn btn-primary me-2">Sign up</a>
                <a href="/login" class="btn btn-outline-primary">Log in</a>
            </div>
        <% } else { %>
            <!-- If user is logged in they can logout or go to members area -->
            <div class="text-center mt-4">
                <p>Hello, <span id="userName"><%= name %></span>!</p>
                <a href="/members" class="btn btn-success me-2">Go to Members Area</a>
                <a href="/admin" class="btn btn-info">Go to Admin Page</a>
                <a href="/logout" class="btn btn-danger">Logout</a>
            </div>
        <% } %>
    </div>
    <!-- Including the footer-->
     <%- include('footer') %>
</body>
</html>