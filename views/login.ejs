<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In</title>
    <!-- Linking Bootstrap to the file -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        /* All custom styles needed beyond Bootstrap */
        body {
            font-family: sans-serif;
            margin: 20px;
        }
        .error-message {
            color: red; 
            margin-top: 1rem;
        }
        .try-again {
            margin-top: 1rem; 
            text-align: center;
        }
        .try-again a {
            color: #007bff; 
            text-decoration: none; 
        }
        .try-again a:hover {
            text-decoration: underline; 
        }
    </style>
</head>

<body>

    <div class="container">
        <h1 class="text-center mb-4">Log In</h1>
            <!-- If an error occurrs during login stage -->
        <% if (errorMessage) { %>
            <p class="error-message"><%= errorMessage %></p>
            <div class="try-again">
                <a href="/login" class="link-primary">Try again</a>
            </div>

            <% } else { %>
                <!-- Login form shown to the user with required fields -->
                <form action="/login" method="POST">
                    <div class="mb-3">
                        <label for="email" class="form-label">Email:</label>
                        <input type="email" class="form-control" id="email" name="email" required> 
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Password:</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>

                   <button type="submit" class="btn btn-primary">Submit</button>
                </form>
                <% } %>
    </div>
</body>

</html>